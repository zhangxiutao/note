所有点里面可能有好几个Gaussian model。
1.已知点的类型，推断模型参数。
2.已知模型参数，推断点的类型。
EM要解决的是，既不知道模型参数，也不知道点的类型的情况下，要同时推断点的类型
与模型参数。
curb检测也是一样，我不知道curb在哪里，也不知道哪些是curb。但是我要同时
推断出参数与类型

soft clustering，每个点得到的是不同类型的概率。

把任务看成什么问题决定了解决的方式。可以是聚类，可以是选择。
动态规划，贪心算法，bt只是在解空间很大的时候的一种搜索策略，但是问题
还是一样的

我一开始想的，先竖直分割成好几列，把某列的点全部想成是curb,然后挑一个模型
或者拟合。本质上就是我先决定了点的类型再推断模型参数

ransac也是，随机选择模型，然后挑最好的模型。
Kmeans也是，随机选择模型，然后分类，由分好的类选择新模型。
EM也是

这些方法，可以分成两类，一类是KMEANS和EM，模型导致分类，分类生成
新模型。

另一类是RANSAC，穷举模型，平行比较。
另一类是霍夫变换，穷举模型平行比较。

https://www.youtube.com/watch?v=REypj2sy_5U&list=PLBv09BD7ez_4e9LtmK626Evn1ion6ynrt&index=1

可以借鉴的思想是，我应该先选择一个模型，然后要么用一类要么用二类。